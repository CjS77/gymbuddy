[package]
name = "gymbuddy-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Wire protocol DTOs shared by the GymBuddy server and all clients"

[dependencies]
serde = { workspace = true }
postcard = { workspace = true }
